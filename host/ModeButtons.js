import React, { Component } from 'react'
import { connect } from 'react-redux'

const modes = ["wait", "description", "auction", "result"]

const mapStateToProps = ({}) => ({
})

class ModeButtons extends Component {
  render() {
    return (
      <div>
      </div>
    )
  }
}

export default connect()(ModeButtons)
